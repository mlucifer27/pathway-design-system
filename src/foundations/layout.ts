/** Default max width for authenticated mobile-first dashboards (AthleteIQ). */
export const DASHBOARD_CONTAINER_MAX_W = "2xl" as const;

export const DASHBOARD_CONTAINER_PX = "4" as const;

/** Bottom padding above fixed bottom nav on dashboard routes. */
export const DASHBOARD_CONTAINER_PB = "24" as const;

/** Marketing / public content width (Netfusion ~1140px → Chakra 6xl). */
export const MARKETING_CONTAINER_MAX_W = "6xl" as const;
